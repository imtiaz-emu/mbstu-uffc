<h1>Fixtures</h1>

<h3>Fixtures: BORgin_MBSTU</h3>

<table class="table table-hover">
  <thead>
  <tr>
    <th># Gameweek</th>
    <th>Home Team</th>
    <th>Away Team</th>
    <th>Actions</th>
  </tr>
  </thead>

  <tbody>
  <% @fixtures_ohid.each do |fixture| %>
    <tr>
      <th scope="row"><%= fixture['gameweek'] %></th>
      <td><%= fixture['home_team'] %></td>
      <td><%= fixture['away_team'] %></td>
      <td>
        <a href="<%= bor_details_path('ohid', fixture['gameweek'], result: true) %>" data-turbolinks="false" class="btn btn-outline-primary" role="button">Results</a>
        <a href="<%= bor_details_path('ohid', fixture['gameweek']) %>" data-turbolinks="false" class="btn btn-outline-primary" role="button">Teams</a>
      </td>
    </tr>
  <% end %>
  </tbody>
</table>

<h3>Fixtures: ঊনপঞ্চাশ বায়ু</h3>

<table class="table table-hover">
  <thead>
  <tr>
    <th># Gameweek</th>
    <th>Home Team</th>
    <th>Away Team</th>
    <th>Actions</th>
  </tr>
  </thead>

  <tbody>
  <% @fixtures_khaled.each do |fixture| %>
    <tr>
      <th scope="row"><%= fixture['gameweek'] %></th>
      <td><%= fixture['home_team'] %></td>
      <td><%= fixture['away_team'] %></td>
      <td>
        <a href="<%= bor_details_path('khaled', fixture['gameweek'], result: true) %>" data-turbolinks="false" class="btn btn-outline-primary" role="button">Results</a>
        <a href="<%= bor_details_path('khaled', fixture['gameweek']) %>" data-turbolinks="false" class="btn btn-outline-primary" role="button">Teams</a>
      </td>
    </tr>
  <% end %>
  </tbody>
</table>

